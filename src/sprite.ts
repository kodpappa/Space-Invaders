class Sprite {
  offset: Position;
  width: number;
  height: number;

  constructor(offset: Position, width: number, height: number) {
    this.offset = offset;
    this.width = width;
    this.height = height;
  }
}
